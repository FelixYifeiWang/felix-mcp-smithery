kind: server
name: felix-mcp-smithery
version: 1.0.0
runtime: container

startCommand:
  type: http

transport: streamable-http
port: 8081
path: /mcp      # POST/GET/DELETE happen on the same path
ssePath: /mcp
health: /
