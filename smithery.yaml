kind: server
name: felix-mcp-smithery
version: 1.0.0
runtime: container

startCommand:
  type: http

transport: streamable-http
port: 8081
path: /mcp        # POST/GET/DELETE
ssePath: /mcp     # (Smithery uses same path for SSE when streamable-http)
health: /
